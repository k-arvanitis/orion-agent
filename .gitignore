# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment variables
.env

# Gmail OAuth — generated locally, never commit
credentials.json
token.json

# Data — CSVs are large and loaded into Supabase, not needed in repo
data/customer-data/
data/output/

# Misc artefacts
chunks.json

# Eval run outputs — only the current results are tracked
eval/*.json
eval/*.log
eval*.log
nohup.out

# Conversation memory (SQLite, generated at runtime)
data/memory.db

# IDE
*.code-workspace
.vscode/
data/checkpoints.db*
data/orion_support.db

# Local planning docs
TODO.md
PROGRESS.md

# Local Claude Code project instructions — not for the public repo
CLAUDE.md
UPWORK_PITCH.md

# Internal market-fit notes, not for the public repo
docs/UPWORK_PORTFOLIO_FIT.md
